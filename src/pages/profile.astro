---
import Layout from "../layouts/Layout.astro";
import { Navigation } from "../components/navigation/Navigation";
import { ProfileManager } from "../components/auth/ProfileManager";

// Auth check - middleware już przekierowuje niezalogowanych użytkowników
const { user } = Astro.locals;

// Dodatkowa walidacja (middleware powinno to już obsłużyć)
if (!user) {
  return Astro.redirect("/auth/login");
}

const currentPath = Astro.url.pathname;

// Format user data for display
const userData = {
  id: user.id,
  email: user.email || "",
  email_confirmed: !!user.email_confirmed_at,
  created_at: user.created_at ? new Date(user.created_at) : new Date(),
};
---

<Layout title="Profil - 10xCards">
  <Navigation user={userData} currentPath={currentPath} client:load />

  <main class="min-h-screen bg-background">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <!-- Page Header -->
      <div class="mb-8">
        <h1 class="text-2xl font-bold text-foreground sm:text-3xl">Profil użytkownika</h1>
        <p class="mt-2 text-muted-foreground">Zarządzaj swoim kontem i preferencjami</p>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
        <!-- Main Content -->
        <div class="lg:col-span-2 space-y-6">
          <!-- Profile Manager with Interactive Components -->
          <ProfileManager user={userData} client:load />

          <!-- Learning Preferences -->
          <div class="bg-card border border-border rounded-xl p-6">
            <h2 class="text-lg font-semibold text-card-foreground mb-6">Preferencje nauki</h2>

            <div class="space-y-6">
              <!-- Daily Goal -->
              <div>
                <label class="block text-sm font-medium text-foreground mb-3"> Dzienny cel nauki </label>
                <div class="grid grid-cols-3 gap-2">
                  <button
                    class="p-3 border border-border rounded-md text-center hover:border-primary hover:bg-primary/5 transition-colors"
                  >
                    <div class="font-medium text-card-foreground">5</div>
                    <div class="text-xs text-muted-foreground">fiszek</div>
                  </button>
                  <button class="p-3 border border-primary bg-primary/5 text-primary rounded-md text-center">
                    <div class="font-medium">10</div>
                    <div class="text-xs">fiszek</div>
                  </button>
                  <button
                    class="p-3 border border-border rounded-md text-center hover:border-primary hover:bg-primary/5 transition-colors"
                  >
                    <div class="font-medium text-card-foreground">20</div>
                    <div class="text-xs text-muted-foreground">fiszek</div>
                  </button>
                </div>
              </div>

              <!-- Notifications -->
              <div>
                <h3 class="text-sm font-medium text-foreground mb-3">Powiadomienia</h3>
                <div class="space-y-3">
                  <label class="flex items-center justify-between">
                    <span class="text-sm text-card-foreground">Przypomnienia o nauce</span>
                    <input type="checkbox" checked class="rounded border-border text-primary focus:ring-primary/20" />
                  </label>
                  <label class="flex items-center justify-between">
                    <span class="text-sm text-card-foreground">Nowe funkcje</span>
                    <input type="checkbox" class="rounded border-border text-primary focus:ring-primary/20" />
                  </label>
                  <label class="flex items-center justify-between">
                    <span class="text-sm text-card-foreground">Wskazówki nauki</span>
                    <input type="checkbox" checked class="rounded border-border text-primary focus:ring-primary/20" />
                  </label>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Sidebar -->
        <div class="space-y-6">
          <!-- Quick Stats -->
          <div class="bg-card border border-border rounded-xl p-6">
            <h3 class="font-semibold text-card-foreground mb-4">Twoje statystyki</h3>
            <div class="space-y-4">
              <div class="text-center p-3 bg-muted/50 rounded-md">
                <div class="text-2xl font-bold text-card-foreground">0</div>
                <div class="text-xs text-muted-foreground">Fiszki ogółem</div>
              </div>
              <div class="text-center p-3 bg-muted/50 rounded-md">
                <div class="text-2xl font-bold text-card-foreground">0</div>
                <div class="text-xs text-muted-foreground">Dni nauki</div>
              </div>
              <div class="text-center p-3 bg-muted/50 rounded-md">
                <div class="text-2xl font-bold text-card-foreground">0%</div>
                <div class="text-xs text-muted-foreground">Skuteczność</div>
              </div>
            </div>
          </div>

          <!-- Recent Activity -->
          <div class="bg-card border border-border rounded-xl p-6">
            <h3 class="font-semibold text-card-foreground mb-4">Ostatnia aktywność</h3>
            <div class="text-center py-4">
              <div
                class="mx-auto h-8 w-8 flex items-center justify-center rounded-full bg-muted text-muted-foreground mb-3"
              >
                <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M9 5H7a2 2 0 00-2 2v10a2 2 0 002 2h8a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"
                  ></path>
                </svg>
              </div>
              <p class="text-sm text-muted-foreground">Brak aktywności</p>
            </div>
          </div>

          <!-- Help & Support -->
          <div class="bg-card border border-border rounded-xl p-6">
            <h3 class="font-semibold text-card-foreground mb-4">Pomoc i wsparcie</h3>
            <div class="space-y-3">
              <a href="#" class="block text-sm text-primary hover:underline"> 📚 Przewodnik dla początkujących </a>
              <a href="#" class="block text-sm text-primary hover:underline"> 💬 Skontaktuj się z pomocą </a>
              <a href="#" class="block text-sm text-primary hover:underline"> 🔒 Polityka prywatności </a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </main>
</Layout>
